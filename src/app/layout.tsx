import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { ThemeProvider } from '@/components/theme-provider';
import { Toaster } from '@/components/ui/toaster';
import '@/locales/i18n';

const inter = Inter({ subsets: ['latin', 'thai'] });

export const metadata: Metadata = {
  title: 'SDS Manager - Safety Data Sheet Management System',
  description: 'A production-ready system for managing Safety Data Sheets (SDS/MSDS) for industrial chemicals and maintenance products.',
  icons: {
    icon: '/favicon.ico',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}