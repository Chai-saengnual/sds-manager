# Cursor Cloud Agent sessions

Sessions moved to a dedicated repository:

**https://github.com/Chai-saengnual/Cursor-AI-Oncloud**

The former `packages/Cursor-AI-Oncloud/` copy was removed from sds-manager. Use that repo for:

- `sessions/facebook-article-content/` — Facebook → Notion **Chai Article**
- `sessions/_template/` — scaffold for new sessions
- `scripts/new-session.sh` — create a new session folder
